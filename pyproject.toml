[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "cedarpoint-ml-utils"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["python-dateutil>=2.8.2"]

[tool.setuptools.packages.find]
where = ["src"]
